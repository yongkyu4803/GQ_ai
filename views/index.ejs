<!-- index.ejs -->
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>생성형 AI 이해하기</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Nanum+Gothic:wght@400;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" as="style" crossorigin href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.8/dist/web/static/pretendard.css" />
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <%- include('navbar') %>

    <div id="content">
        <!-- 랜딩 섹션 -->
        <section id="landing">
            <div class="container text-center landing-container">
                <h1>Welcome to the Generative AI Lecture</h1>
                <p>Explore how Generative AI can transform your work and solve critical challenges.</p>
            </div>
        </section>


        <!-- 강의 섹션 -->
        <section id="lectures" class="py-4">         
            <div class="container">
                <!-- 🎯 기초 단계 -->
                <div class="section-header">
                    <h2 class="section-title basic">🎯 기초</h2>
                    <p class="section-subtitle">AI 기본 개념</p>
                </div>
                
                <div class="row">                 
                    <!-- DEBUG: 확인용 lectures 출력 -->
                    <script>
                      console.log("DEBUG lectures:", <%- JSON.stringify(lectures) %>);
                    </script>
                    
                    <!-- 1-2강: 기초 단계 -->
                    <% 
                    const basicLectures = lectures.slice(0, 2);
                    const basicIcons = ['fa-brain', 'fa-users'];
                    const basicTimes = ['45분', '50분'];
                    basicLectures.forEach(function(lecture, index) { %>
                        <div class="col-md-6 col-lg-4 mb-3">
                            <div class="card">
                                <div class="card-header-custom basic">
                                    <div class="card-header-icon">
                                        <i class="fas <%= basicIcons[index] %>"></i>
                                    </div>
                                    <h3 class="card-title-header"><%= lecture.number %>. <%= lecture.title %></h3>
                                </div>
                                <div class="card-body">
                                    <div class="card-content">
                                        <p class="card-text"><%= lecture.description %></p>
                                        <div class="card-meta">
                                            <div class="meta-item">
                                                <i class="fas fa-clock"></i><span><%= basicTimes[index] %></span>
                                            </div>
                                            <div class="meta-item">
                                                <i class="fas fa-signal"></i><span>초급</span>
                                            </div>
                                        </div>
                                    </div>
                                    <a class="btn-outline-small" href="<%= lecture.link %>">
                                        <i class="fas fa-play"></i>강의보기
                                    </a>
                                </div>
                                <div class="lecture-number"><%= index + 1 %></div>
                            </div>
                        </div>
                    <% }); %>
                </div>

                <!-- ⚡ 중급 단계 -->
                <div class="section-header">
                    <h2 class="section-title">⚡ 중급</h2>
                    <p class="section-subtitle">프롬프트 엔지니어링</p>
                </div>
                
                <div class="row">
                    <!-- 3-4강: 중급 단계 -->
                    <% 
                    const intermediateLectures = lectures.slice(2, 4);
                    const intermediateIcons = ['fa-terminal', 'fa-cogs'];
                    const intermediateTimes = ['60분', '55분'];
                    intermediateLectures.forEach(function(lecture, index) { %>
                        <div class="col-md-6 col-lg-4 mb-3">
                            <div class="card">
                                <div class="card-header-custom intermediate">
                                    <div class="card-header-icon">
                                        <i class="fas <%= intermediateIcons[index] %>"></i>
                                    </div>
                                    <h3 class="card-title-header"><%= lecture.number %>. <%= lecture.title %></h3>
                                </div>
                                <div class="card-body">
                                    <div class="card-content">
                                        <p class="card-text"><%= lecture.description %></p>
                                        <div class="card-meta">
                                            <div class="meta-item">
                                                <i class="fas fa-clock"></i><span><%= intermediateTimes[index] %></span>
                                            </div>
                                            <div class="meta-item">
                                                <i class="fas fa-signal"></i><span>중급</span>
                                            </div>
                                        </div>
                                    </div>
                                    <a class="btn-outline-small" href="<%= lecture.link %>">
                                        <i class="fas fa-play"></i>강의보기
                                    </a>
                                </div>
                                <div class="lecture-number"><%= index + 3 %></div>
                            </div>
                        </div>
                    <% }); %>
                </div>

                <!-- 🚀 활용 단계 -->
                <div class="section-header">
                    <h2 class="section-title">🚀 활용</h2>
                    <p class="section-subtitle">실무 적용</p>
                </div>
                
                <div class="row">
                    <!-- 5-7강: 활용 단계 -->
                    <% 
                    const practicalLectures = lectures.slice(4, 7);
                    const practicalIcons = ['fa-masks-theater', 'fa-file-alt', 'fa-pen-fancy'];
                    const practicalTimes = ['40분', '50분', '45분'];
                    practicalLectures.forEach(function(lecture, index) { %>
                        <div class="col-md-6 col-lg-4 mb-3">
                            <div class="card">
                                <div class="card-header-custom practical">
                                    <div class="card-header-icon">
                                        <i class="fas <%= practicalIcons[index] %>"></i>
                                    </div>
                                    <h3 class="card-title-header"><%= lecture.number %>. <%= lecture.title %></h3>
                                </div>
                                <div class="card-body">
                                    <div class="card-content">
                                        <p class="card-text"><%= lecture.description %></p>
                                        <div class="card-meta">
                                            <div class="meta-item">
                                                <i class="fas fa-clock"></i><span><%= practicalTimes[index] %></span>
                                            </div>
                                            <div class="meta-item">
                                                <i class="fas fa-signal"></i><span>중급</span>
                                            </div>
                                        </div>
                                    </div>
                                    <a class="btn-outline-small" href="<%= lecture.link %>">
                                        <i class="fas fa-play"></i>강의보기
                                    </a>
                                </div>
                                <div class="lecture-number"><%= index + 5 %></div>
                            </div>
                        </div>
                    <% }); %>
                </div>

                <!-- 💎 고급 · 전문 단계 (통합) -->
                <div class="section-header">
                    <h2 class="section-title">💎 고급 · 전문</h2>
                    <p class="section-subtitle">프로젝트 & 코딩</p>
                </div>
                
                <div class="row">
                    <!-- 8-11강: 고급/전문 단계 + Q&A -->
                    <% 
                    const advancedLectures = lectures.slice(7, 11);
                    const advancedIcons = ['fa-tools', 'fa-project-diagram', 'fa-cog', 'fa-code'];
                    const advancedTimes = ['55분', '60분', '35분', '70분'];
                    const advancedLevels = ['고급', '고급', '전문', '전문'];
                    const advancedClasses = ['advanced', 'advanced', 'expert', 'expert'];
                    
                    advancedLectures.forEach(function(lecture, index) { %>
                        <div class="col-md-6 col-lg-4 mb-3">
                            <div class="card">
                                <div class="card-header-custom <%= advancedClasses[index] %>">
                                    <div class="card-header-icon">
                                        <i class="fas <%= advancedIcons[index] %>"></i>
                                    </div>
                                    <h3 class="card-title-header"><%= lecture.number %>. <%= lecture.title %></h3>
                                </div>
                                <div class="card-body">
                                    <div class="card-content">
                                        <p class="card-text"><%= lecture.description %></p>
                                        <div class="card-meta">
                                            <div class="meta-item">
                                                <i class="fas fa-clock"></i><span><%= advancedTimes[index] %></span>
                                            </div>
                                            <div class="meta-item">
                                                <i class="fas fa-signal"></i><span><%= advancedLevels[index] %></span>
                                            </div>
                                        </div>
                                    </div>
                                    <a class="btn-outline-small" href="<%= lecture.link %>">
                                        <i class="fas fa-play"></i>강의보기
                                    </a>
                                </div>
                                <div class="lecture-number"><%= index + 8 %></div>
                            </div>
                        </div>
                    <% }); %>

                    <!-- Q&A 카드 (특별) -->
                    <div class="col-md-6 col-lg-4 mb-3">                     
                        <div class="card">
                            <div class="card-header-custom special">
                                <div class="card-header-icon">
                                    <i class="fas fa-question-circle"></i>
                                </div>
                                <h3 class="card-title-header">Q&amp;A</h3>
                            </div>                         
                            <div class="card-body">     
                                <div class="card-content">                        
                                    <p class="card-text">질문을 통해 이해도를 높이세요.</p>
                                    <div class="card-meta">
                                        <div class="meta-item">
                                            <i class="fas fa-comments"></i><span>상시</span>
                                        </div>
                                        <div class="meta-item">
                                            <i class="fas fa-users"></i><span>커뮤니티</span>
                                        </div>
                                    </div>
                                </div>
                                <a class="btn-outline-small" href="/qna">
                                    <i class="fas fa-arrow-right"></i>질문하기
                                </a>                         
                            </div>
                            <div class="lecture-number">Q&A</div>                     
                        </div>                 
                    </div>
                </div>
            </div>     
        </section>

        <!-- 푸터 섹션 -->
        <footer class="py-4" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white;">
            <div class="container">
                <div class="row align-items-center">
                    <div class="col-md-6 text-center text-md-start">
                        <p class="mb-0" style="font-family: 'Montserrat', sans-serif; letter-spacing: 0.5px;">
                            <i class="fas fa-star me-1"></i> Designed with <i class="fas fa-heart mx-1" style="color: #ff6b6b;"></i> by GQ
                        </p>
                    </div>
                    <div class="col-md-6 text-center text-md-end mt-3 mt-md-0">
                        <p class="mb-0" style="font-size: 0.9rem; opacity: 0.8;">
                            © <%= new Date().getFullYear() %> GQ AI Learning Platform. All rights reserved.
                        </p>
                    </div>
                </div>
            </div>
        </footer>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // 토글 가능한 헤딩 기능 추가
        document.addEventListener('DOMContentLoaded', function() {
            const toggleHeadings = document.querySelectorAll('.toggleable .toggle-head');
            
            toggleHeadings.forEach(heading => {
                // 초기 상태 설정
                if(heading.classList.contains('active')) {
                    const content = heading.nextElementSibling;
                    if(content && content.classList.contains('toggle-content')) {
                        content.style.display = 'block';
                    }
                }
                
                heading.addEventListener('click', function() {
                    this.classList.toggle('active');
                    const content = this.nextElementSibling;
                    
                    if(content && content.classList.contains('toggle-content')) {
                        if(this.classList.contains('active')) {
                            content.style.display = 'block';
                            content.style.animation = 'fadeIn 0.5s ease';
                        } else {
                            content.style.display = 'none';
                        }
                    }
                });
            });
        });
    </script>
</body>
</html>
